apiVersion: v1
kind: Service
metadata:
  name: portfolio-app
  namespace: portfolio
  labels:
    app: portfolio-app
spec:
  type: NodePort
  selector:
    app: portfolio-app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
